package com.example.bingocastrobackend.Repository.Dashboard;

import com.example.bingocastrobackend.Model.Dashboard.GameLetter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GameLetterRepository extends JpaRepository<GameLetter, Integer> {
}
